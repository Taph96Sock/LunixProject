sudo: required

dist: trusty

os:
  - windows
  - linux
  - osx

language: java
jdk:
- oraclejdk8

script: bundle exec rake build && bundle exec rake builddoc

notifications:
 email: moustaphasock813@gmail.com
